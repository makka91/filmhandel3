import { SearchFormatPipe } from './search-format.pipe';

describe('SearchFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
