import { SearchGenrePipe } from './search-genre.pipe';

describe('SearchGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
